import 'package:flutter/material.dart';

class VoiceCallScreenScreen extends StatelessWidget {
  const VoiceCallScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('JP Chat Talk,Fun,Chat')),
      body: const Center(
        child: Text('Voice Call Screen - Coming Soon!'),
      ),
    );
  }
}
