import 'package:flutter/material.dart';

class VideoCallScreenScreen extends StatelessWidget {
  const VideoCallScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('JP Chat Talk,Fun,Chat')),
      body: const Center(
        child: Text('Video Call Screen - Coming Soon!'),
      ),
    );
  }
}
