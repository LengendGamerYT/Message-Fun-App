name: jp_chat_app
description: "JP Chat Talk,Fun,Chat - Complete messaging app with username-based contacts, message deletion, and file attachments"
publish_to: 'none'

version: 2.0.0+2

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core Dependencies
  cupertino_icons: ^1.0.6
  
  # State Management
  provider: ^6.1.1
  get: ^4.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true